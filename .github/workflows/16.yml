name: Code Checkout-Other Repo
on: workflow_dispatch
jobs:
  jobx:
    runs-on: ubuntu-latest
    steps:
    - name: Check out
      uses: actions/checkout@v7  
      with:
        repository: smaranit/MBAPP
    - name: display  file
      run: |
         ls -l 
               
  
    


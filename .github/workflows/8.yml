name: WF-8
on: workflow_dispatch
jobs:
  jobx:
    runs-on: ubuntu-latest
    steps:
    - name: step1
      run: |
         hostnamectl
    - name: step2
      if: ${{ github.event=='push' }}
      run: |
         echo "Todays date $(date)" 
         
    - name: step3
      run: |
         pwd
         echo " This is Step3"         
        
name: Code Checkout
on: workflow_dispatch
jobs:
  jobx:
    runs-on: ubuntu-latest
    steps:
    - name: Check out
      uses: actions/checkout@v7  
    - name: step2 
      run: |
        pwd
        ls -l         


name: WF-13
on: workflow_dispatch
jobs:
  jobx:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
         os: [ubuntu-latest,ubuntu-slim]
         version: ["3.10","3.11","3.12","3.13"]
    steps: 
    - name: Step1
      run: |
        sudo apt-get update
        sudo apt-get install python${{ matrix.version}} -y  
    - name: Excute application
      run: 
         python --version      
          
          
         
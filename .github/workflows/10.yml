name: Wf-9
on: workflow_dispatch
jobs:
  jobx:
    runs-on: ubuntu-latest
    steps:
    - name: step1
      run: |
          echo "This is step1"
    - name: step2
      if: ${{ github.ref_name=='master'}}
      run: |
          echo "This is ${{ github.ref_name }}"
    - name: step3
      if: ${{ runner.os =='Linux' }}  
      run: |
         echo " Testing "      
     
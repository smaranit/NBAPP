name: WF-6
on: workflow_dispatch
jobs:
  jobx:
    runs-on: ubuntu-slim
    steps:
    - name: Git Workflow context
      run: |
         echo "Actor ${{ github.actor }}"
         echo "Event name is ${{ github.event_name }}"
         echo "Commit id is ${{ github.sha }}"
         echo "Branch Name is ${{ github.ref_name}}"
         echo "Branch Name is ${{ github.ref}}"
         echo "Reponame is ${{github.repository}}"
         echo "Job Run Number is ${{ github.run_number }}"
         echo "**************************************"
         echo "Payload is ${{ toJSON(github.event) }} " 
         echo "Commit Msg is ${{github.event.commits[0].message }}"         
         echo "Authorname is ${{ github.event.commits[0].author.name }}"
        